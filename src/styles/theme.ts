import { AppTheme as AppThemeType } from "../types/theme.type";

/**
 * Theme color definition
 */
export const AppTheme: AppThemeType = {
  backgroundColorLight: "#f0f0ec",
  backgroundColorPrimary: "#ffffff",
  backgroundColorSecondary: "#343a40",
  buttonColorPrimary: "#ffc107",
  buttonTextColorPrimary: "#101820",
  headingColor: "#003e51",
};
