/**
 * API Routes
 */
export const API_ROUTES: {
  baseUrl: string;
  register: string;
  login: string;
  events: string;
} = {
  baseUrl: "https://opendata.hopefully.works/api",
  register: "/signup",
  login: "/login",
  events: "/events",
};
